export default function AutoChart({ columns, rows }) {
  return (
    <div className="p-4 text-sm text-gray-400 flex items-center justify-center h-full">
      AutoChart — coming in sub-phase 4.2
    </div>
  )
}
